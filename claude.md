# CLAUDE.md - FRC Hybrid Refactor Guidelines

## 🤖 System Context
- **Project Type:** FRC Robotics Code (2026/2027 Transition)
- **Team:** 4935 (Trex4935). Both this repo and `C:\Users\xdm\frc-2026\2026` declare `"teamNumber": 4935`. The Obsidian vault folder is named `6767 frc bible` — that's a naming meme, not a team number.
- **User Role:** Mentor overseeing the system architecture.
- **Goal:** Build a modular, simulation-first codebase combining 1678's IO-isolation and 254's state machine.

## 🧠 Permanent Brain Sync (Obsidian Workflow)
- **Vault Location:** `C:\Users\xdm\6767 frc bible`
- **Sync Rule:** Claude reads this file at the start of every session to establish state. At the end of every session, the user invokes `/tldr` to update both this file and append new codebase memories (critical math, calibrations, or fixes) to the central Obsidian Vault.
- **Vault entry point:** `00 Index.md`. Key notes: `Best Practices & Upgrades.md` (peer benchmarking), `Control Loops & Math/PID Gains Registry.md`, `Control Loops & Math/Swerve Offsets & Kinematics.md`, `WPILib & Simulation/Field & Alliance Coordinates.md`.
- The vault is plain markdown — write to it with normal file tools. The Obsidian CLI is **not** installed and is not required.

## 🛠️ Superpowers (Available MCP Tools & Plugins)
- **Context7 (online):** Up-to-date WPILib & JavaDocs API reference layer.
- **WebSearch (built-in search):** Real-time web-search validation engine.
- **WebFetch (built-in scraping):** Remote markdown/URL scraping capability.

## 📂 Reference Directories (`temp_reference/`)
You have read access to elite architectural templates inside these subdirectories. Use them for structural examples, but **never merge or import them directly**:
- `temp_reference/Lynk 2026/`
- `temp_reference/Phenoix 6 API Examples/` *(yes, misspelled on disk)*
- `temp_reference/Team 254 Code/`
- `temp_reference/Team 1678 Code/`
- `temp_reference/Team 6328 Code/`

A larger corpus also lives at `C:\Users\xdm\frc-steal-from-the-best\` (254, 1678, 6328/MechAdv, Lynk, YAMS, Phoenix 6 examples). Same rule: read, never merge.

> **Never copy numbers out of reference code.** Their masses, gains and field constants describe their robot and their year. Copy patterns, rewrite against our `Constants` / `FieldConstants`.

## 🛠️ Build & Test Commands
- **Compile Java:** `./gradlew compileJava`
- **Build Robot:** `./gradlew build`
- **Run Simulator:** `./gradlew simulateJava`

**JDK requirement:** the machine's PATH `java` is JDK 25, which Gradle 8.11 cannot run — every task dies during configuration with `Could not create task ':test'` / `Type T not present`. This is an environment fault, not a code fault. Export the WPILib JDK 17 first:

```bash
export JAVA_HOME="/c/Users/Public/wpilib/2026/jdk"   # Temurin 17.0.16
```

## 📐 Architecture Rules
1. **Strict Hardware Isolation:** No vendor hardware APIs (CTRE Phoenix, REVLib, etc.) are allowed in standard subsystem files. They must live strictly inside `*IOReal.java` implementations.
   - **Sanctioned vendor exception (decided 2026-07-16):** `subsystems/CommandSwerveDrivetrain.java` and `utility/simulation/MapleSimSwerveDrivetrain.java` are the **singular** sanctioned exception to this rule. They are CTRE Tuner X / swerve-generator output and stay vendor-inline rather than wrapped in a `DriveIO` — regenerating them from Tuner X would otherwise conflict with a hand-maintained IO wrapper. Every other file must stay vendor-free.
   - **Utility layer is NOT exempt.** `utility/HubActiveState.java` used to import `com.ctre.phoenix6` (`HootAutoReplay`, `Utils`) directly for Hoot auto-log/auto-replay. That hookup is now isolated in `utility/HootReplayBridge.java` (package-private, the only file in `utility/` that touches Phoenix 6); `HubActiveState.java` itself is vendor-free.
2. **Singleton Subsystems:** Every subsystem and the `Superstructure` must implement the Singleton pattern (`private` constructor and `public static Subsystem getInstance()`).
3. **Centralized States:** The `Superstructure` coordinates complex multi-subsystem states via a `SuperstructureState` enum (`OFF`, `INTAKING`, `EJECTING`, `STOWED`, `ALIGNING`, `SHOOTING`) and a `periodic()` switch-case that commands Shooter/Intake/Vision directly. `shootCmd()`/`shootingSequence(double)` are thin Command bridges kept only because RobotContainer's `whileTrue` binding and PathPlanner's NamedCommands both require real Command objects — the actual arbitration logic lives in `periodic()`, not in composed Commands. Subsystems themselves still only handle their own immediate mechanism control (e.g. `Intake.agitatePivot()` stays owned by Intake; Superstructure schedules/cancels it as a unit rather than reimplementing its oscillation).

## 🧠 Karpathy Guidelines (Anti-Failure Mode Protocol)
To ensure elite execution, strictly adhere to these behaviors:
- **No Assumptions:** Never guess vendor API syntax or class constructor shapes (especially Phoenix 6 vs 5). Stop and use `context7` or web search to verify the real-world signature first.
- **Simplicity First (Anti-Overengineering):** Write the cleanest, most minimalist code required to satisfy the immediate structural requirement or simulation goal.
- **Surgical Changes Only:** Modify *only* the specific files related to the active task. Do not rewrite, clean up, or change formatting in adjacent methods or unrelated classes.
- **Never Declare Success Early:** A task is not complete until you explicitly run `./gradlew compileJava` via the shell tool and confirm a zero-error output. If it breaks, fix it immediately.
- **Don't Invent Files:** If the user references a class that does not exist in `src/` (e.g. `FuelSim`), say so and stop. Do not create it from a reference-directory copy, and do not edit files under `temp_reference/`.

## 🕒 Current Task State
*Verified against the tree on 2026-07-17 (third session) at commit `87b1e39` (working tree clean; only untracked file is `docs/superpowers/plans/2026-07-17-phase3-imu-mode-junit-test.md`). `./gradlew compileJava`: BUILD SUCCESSFUL, re-run this session. `./gradlew test --tests "frc.robot.RobotLifecycleTest"`: BUILD SUCCESSFUL, 1/1 PASSED, re-run this session.*

### Done
- **Intake refactor:** complete. `subsystems/intake/` holds `IntakeIO.java`, `IntakeIOReal.java`, `IntakeIOSim.java`, `Intake.java` (Singleton via `getInstance()`, zero vendor imports).
- **Shooter refactor:** complete. `subsystems/shooter/` holds `ShooterIO.java`, `ShooterIOReal.java`, `ShooterIOSim.java`, `Shooter.java` (Singleton via `getInstance()`, zero vendor imports).
- **Vision/Limelight isolation — complete end-to-end (Phase 2 + Phase 3, 2026-07-16):** `subsystems/vision/` holds `VisionIO.java`, `VisionIOReal.java`, `VisionIOSim.java`, `Vision.java` (Singleton via `getInstance(drivetrain)`). `LimelightHelpers` is imported only by `VisionIOReal.java`. Phase 3 closed the last leak: `VisionIO` gained `setIMUMode(int)`/`setIMUAssistAlpha(double)`; `Robot.java` now goes through `Vision.getInstance(...)` exclusively (no `LimelightHelpers` import anywhere outside `VisionIOReal.java`).
- **Superstructure — now a real 254-style state machine (2026-07-16):** Singleton via `getInstance(drivetrain)`. `SuperstructureState` enum (`OFF`/`INTAKING`/`STOWED`/`ALIGNING`/`SHOOTING`) + `periodic()` switch-case arbitration commands Shooter/Intake/Vision directly each tick (RPM targeting, indexer/agitator control, align→shoot phase transitions with teleop-vs-auto timing). `shootCmd()`/`shootingSequence(double)` are thin Command bridges (`requestShoot()`/`requestStow()` underneath) kept only because RobotContainer's `whileTrue` binding and PathPlanner's NamedCommands both require real Command objects — no change needed in `RobotContainer.java`.
- **Utility vendor isolation (2026-07-16):** `utility/HubActiveState.java` no longer imports `com.ctre.phoenix6` directly. The Hoot auto-log/auto-replay hookup (`HootAutoReplay`, `Utils`) is isolated in new `utility/HootReplayBridge.java` (package-private, the sole vendor-touching file in `utility/`).
- **`CommandSwerveDrivetrain` vendor-exception decision (2026-07-16):** recorded in Architecture Rule 1 — `CommandSwerveDrivetrain.java` / `MapleSimSwerveDrivetrain.java` are the singular sanctioned Phoenix 6 exception, left un-wrapped.
- **Deprecation & hygiene fixes (2026-07-16):** `AprilTagFields.loadAprilTagLayoutField()` replaced with `AprilTagFieldLayout.loadField(AprilTagFields)` at `FieldConstants.java:20` and `Vision.java:35`. Stale `@Logged` (Epilogue) annotation removed from `Robot.java`. The 3 dead `SmartDashboard` writes in `RobotContainer.java` moved into a new `RobotContainer.periodic()` called from `Robot.robotPeriodic()`, so they publish live instead of once at boot. `ShooterIOSim`'s agitator now uses `DCMotor.getNeo550(1)` (confirmed real hardware via `ShooterIOReal`'s `MotorArrangementValue.NEO550_JST`) instead of the `DCMotor.getNEO()` placeholder — the factory method exists in this WPILib version; the prior code comment claiming otherwise was wrong.
- **CANcoder boot barrier (2026-07-16, commit `b181bcf`):** `CommandSwerveDrivetrain.java` — all 3 constructors call `logCancoderBootReadings()`: blocks up to 1.5 s in `BaseStatusSignal.waitForAll` on all 4 CANcoder absolute-position signals (real hardware only; sim-safe early return since the maple-sim thread isn't running at construction), prints each module's first fresh reading, logs `Drive/CancoderBootReadRotations/{FL,FR,BL,BR}`, and raises a `DriverStation` warning (without failing boot) on timeout. Readings are offset-adjusted → aligned-straight wheels should log ≈ 0; diagnosis procedure for the FL/BR offset hypothesis is in the vault (`Swerve Offsets & Kinematics.md`). Offsets deliberately untouched.
- **Superstructure v2 + clean RobotContainer routing (2026-07-17, commit `b181bcf`):** `EJECTING` added to `SuperstructureState` (folds the old button-3 intake-eject and button-4 indexer-eject into one state); new factories `intakeCmd()`/`ejectCmd()`/`stowCmd()` alongside `shootCmd()`, all requiring the Superstructure (scheduler now enforces mutual exclusion between them). `STOWED`/`OFF` now command `Roller.STOP` — fixed a latent bug where nothing stopped the intake roller on stow once the old `onFalse` bindings were removed. `RobotContainer` operator board is pure routing: button 2 → `shootCmd()`, 6 → `intakeCmd()`, 3 → `ejectCmd()`; buttons 1/4/5 unbound (manual fixed-RPM shooting moved behind the existing "Shooter Tuning Mode" dashboard toggle, which `shootCmd()` honors); sim controller mirrors (A/X/B); `ctrlBtn` flag and all inline RPM macros deleted.
- **RobotContainer dashboard cleanup + auto IMU-mode fix (2026-07-17, commit `f6df1ef`):** `RobotContainer.dashboard()` no longer exposes raw subsystem `InstantCommand`s (Index Run/Stop/Eject, Stop Shooter, Agitator ±, Set Pivot Up/Down, Intake/Intake Stop) that could fight the Superstructure's `periodic()` arbitration; replaced with 4 buttons that route through the state machine (`State: Intake/Eject/Stow/Shoot` → `superstructure.intakeCmd()/ejectCmd()/stowCmd()/shootCmd()`). Removed the now-unused `InstantCommand`, `Intake.Roller`, `Intake.PivotState`, `Shooter.indexing` imports. `NamedCommands` `"Intake Start Sequence"`/`"Intake Stop"` now carry an explicit code comment recording why they stay on the legacy mechanism-level `Intake` calls (jam-reverse protection isn't migrated yet — decision unchanged, matches the existing Backlog note below). **Root-caused and fixed the actual MegaTag2 auto-snap bug:** verified before touching anything that the continuous per-camera orientation feed (`Vision.periodic()` → `io.setRobotOrientation`, `Vision.java:244`) was already correct from the 2026-07-16 session — the real gap was that `Robot.autonomousInit()` never switched the Limelight off `disabledPeriodic`'s `IMUMode(1)` (EXTERNAL_SEED), so auto ran the whole match in seed mode instead of fusion mode. Added `m_vision.setIMUMode(4)` + `setIMUAssistAlpha(0.001)` to `autonomousInit()` and `setIMUMode(1)` to `disabledInit()` (both `Robot.java`) for immediacy. **Not sim-verified** — no headless/sim run happened this session to confirm the mode switch actually stops the snap in practice.
- **Elite Reference Architecture Deep Dive (2026-07-16):** pattern-level audit of all reference codebases written to the vault (`Projects/Elite Reference Architecture Deep Dive.md`, linked from `00 Index.md`). Part I: 254's A\*-solved state graph with robot-measured transition costs, 1678 `Setpoint` value objects + generic homing, 6328 read/write loop split + fault posture, Phoenix 6 signal lifecycle. Part II: WPILib scheduler resource-locking cross-reference (verified vs current frc-docs) and Phoenix 6 performance spec (verified vs current CTRE docs) — including confirmation that our `CommandSwerveDrivetrain.java:571-611` already applies `Utils.fpgaToCurrentTime()` on vision timestamps (correct as-is; never add a second conversion upstream). Counter-copy rule maintained throughout — patterns only, zero values.
- **PathPlanner `settings.json` partially synced to code (2026-07-16):** `robotTrackwidth`/`driveWheelRadius`/`driveGearing`/`maxDriveSpeed`/`driveCurrentLimit` and all 8 module X/Y fields now match `TunerConstants.java` (the file that actually drives the real swerve hardware, not a placeholder). `robotMass`/`robotMOI`/`wheelCOF`/`robotWidth`/`robotLength`/bumper offsets are untouched — no code-side ground truth exists for them (`Constants.java`'s equivalents are themselves unmeasured `TODO` placeholders); still needs a real-robot measurement pass, see Backlog.
- **SKILLS/ verification toolkit — audited, rewritten, hardened, and run (2026-07-17, commits `9af4206`/`be96e18`/`7791dce`):** `SKILLS/run_headless_sim.py` rewritten (WPILib JDK 17 `JAVA_HOME` export, `gradlew.bat` on Windows, merged/drained output pipes, 420 s launch + 12 s settle windows, `taskkill /T` tree-kill, honest PASS scope statement) and **run twice this session — both PASS, exit 0** (robot init in 12.6–14.3 s, zero exceptions during disabled-mode loops). `build.gradle:95` now gates the sim GUI on `!project.hasProperty('headless')` so `-Pheadless=true` works and interactive `simulateJava` keeps the GUI. `SKILLS/parse_akit_log.py` rewritten as a real pure-stdlib WPILOG v1.0 parser, then hardened against three spec edge cases (timestamp-unordered records, entry-ID reuse after Finish, full array-type decoding) — each proven by a synthetic-log self-test with a planted 0.52 m pose snap that the parser found at the right timestamp. The pre-rewrite `parse_akit_log.py` printed fabricated canned metrics; any historical output from it is fiction. Full lifecycle + honesty rules live in the `## 🔁 Advanced Agent Verification Loop` section at the bottom of this file. `PLAN.md` (repo root) holds the 3-phase blueprint for closing the remaining evidence gaps — Phase 1+2 and Phase 3 Test A are now executed (see the next two bullets); Phase 3 Test B remains, see Next up.
- **PLAN.md Phase 1+2 — SIM-mode log evidence unblocked (2026-07-17, commit `d6456a4`):** `Robot.java:60-63`, SIM case, now adds `Logger.addDataReceiver(new WPILOGWriter("logs"))` alongside the existing `NT4Publisher` (previously SIM only logged to NT4, so a sim run produced no `.wpilog` for `parse_akit_log.py` to read — REAL/REPLAY already had a `WPILOGWriter`, this just closes the SIM gap; same constructor shape, no new API surface). `Vision.java:65-69` gained `currentIMUMode`/`currentIMUAssistAlpha` fields, set inside `setIMUMode(int)`/`setIMUAssistAlpha(double)` (`Vision.java:143-158`), and logged every tick from `periodic()` via `Logger.recordOutput("Vision/IMUMode", currentIMUMode)` / `"Vision/IMUAssistAlpha"` (`Vision.java:251-252`). `logs/` was already in `.gitignore` (WPILib template default) — no change needed there. `SKILLS/run_headless_sim.py` now prints the newest `logs/*.wpilog` path on PASS and takes an optional `--parse` flag chaining straight into `parse_akit_log.py`; that script's own stale docstring (claimed SIM never produces a `.wpilog`, and used entry-name examples missing the required leading `/`) was corrected in the same pass. **Verified:** `./gradlew compileJava` BUILD SUCCESSFUL; `python SKILLS/run_headless_sim.py --parse` PASSED and its own parse of the resulting `.wpilog` showed `/RealOutputs/Vision/IMUMode` = `1` at `t=1.080s` (disabled-mode value, one record only because AdvantageKit dedupes unchanged consecutive outputs and that run never left disabled).
- **PLAN.md Phase 3 Test A — JUnit proof of the IMUMode 1→4 autonomous transition (2026-07-17, commits `a25bc9b`/`40975dc`, subagent-driven per `docs/superpowers/plans/2026-07-17-phase3-imu-mode-junit-test.md`):** `Vision.java:150-152` gained `getIMUMode()` (returns `currentIMUMode`). `build.gradle:92-97`, `test` block, gained `forkEvery = 1` — `Vision`/`Intake`/`Shooter`/`Superstructure` are Singletons with no reset hook, and Gradle's `forkEvery` forks per test **class** (not per method), so this is required to keep a future second test class from silently reusing a stale singleton bound to a different test's drivetrain. New file `src/test/java/frc/robot/RobotLifecycleTest.java` (76 lines, one `@Test` method, deliberately — see the `forkEvery` note above) runs the *real* `Robot.startCompetition()` loop on a background daemon thread rather than calling `disabledInit()`/`autonomousInit()` directly: confirmed by reading AdvantageKit 26.0.2's `LoggedRobot.java` (local sources jar) that `Logger.periodicBeforeUser()`/`periodicAfterUser()` — which flush `recordOutput` calls to the data receivers, including the new SIM `WPILOGWriter` — only run inside that loop, so a direct-call test would have passed without ever writing real log evidence. `@BeforeEach` calls `HAL.initialize(500, 0)`, `DriverStationSim.resetData()`, `SimHooks.pauseTiming()`, constructs `Robot`, starts `robot::startCompetition` on the thread, blocks on `SimHooks.waitForProgramStart()`. The test then: asserts the thread is alive; sets DS disabled/not-autonomous, calls `notifyNewData()`, steps `SimHooks.stepTiming(0.1)`, asserts `Vision.getInstance(null).getIMUMode() == 1`; flips DS to autonomous/enabled, steps again, asserts `getIMUMode() == 4`. (`Vision.getInstance(null)` is safe — by this point `new Robot()` has already constructed the singleton via the real drivetrain; the arg is only consulted on the very first call.) All four WPILib simulation method signatures used (`HAL.initialize(int,int)`, `DriverStationSim.setEnabled/setAutonomous/notifyNewData/resetData`, `SimHooks.pauseTiming/resumeTiming/stepTiming(double)/waitForProgramStart`) were extracted directly from the local `wpilibj-java-2026.2.1-sources.jar`/`hal-java-2026.2.1-sources.jar`, not guessed. **Verified three ways:** (1) `./gradlew test --tests "frc.robot.RobotLifecycleTest"` → BUILD SUCCESSFUL, PASSED. (2) Negative control — expected value temporarily changed `4`→`5`, rerun failed with a real `AssertionFailedError: ... expected: <5> but was: <4>`, then reverted and reconfirmed green — proves the assertion actually discriminates, not just passes by construction. (3) The `.wpilog` the test run produced was independently parsed (`python SKILLS/parse_akit_log.py <file> --dump "/RealOutputs/Vision/IMUMode"`) and showed the real transition from log bytes: `1` at `t=0.002s`, `4` at `t=0.122s` — the same tick `/DriverStation/Enabled` and `/DriverStation/Autonomous` both flip to `True`. **This closes the "not sim-verified" caveat on the 2026-07-17 MegaTag2 auto-snap fix** (`Robot.java:107,127` `setIMUMode(1)`/`setIMUMode(4)` in `disabledInit()`/`autonomousInit()`) — the mode switch is now proven, not just read. `./gradlew test` was wired into the Verification Loop as gate 2.5 (`claude.md`, below).
  - **Honest non-result, not swept under the rug:** the same evidence run's pose-jump analysis (`--pose-entry "/RealOutputs/Odometry/Robot" --jump-threshold 0.15`) reported a real **FAIL** — a 0.2014 m jump at `t=0.04s`. Investigated rather than dismissed: `/RealOutputs/Odometry/Robot` is raw all-zero bytes (`Pose2d.kZero`) at `t=0.002s`, and both DS flags are still `False` at `t=0.04s` (they flip at `t=0.122s`, same tick as the IMU mode transition) — so this is the drivetrain sim settling out of its default zero pose during construction, not a MegaTag2 fusion snap. The pose-*correction* side of the fix is not exercised by this short a test window; only the mode-*switch* side is proven. Don't cite this run as proof the snap itself is gone in general — only that the mode now switches correctly.
- **Sim field/alliance:** `FieldConstants.java` derives field parameters from the `k2026RebuiltAndymark` AprilTag layout and exposes `middleStartFor(Alliance)`. Never hard-code a field length — `16.51` is the 2022–24 field and is wrong for 2026. Gamepiece sim is maple-sim's `SimulatedArena`. **There is no custom project `FuelSim` class**; all simulator adjustments rely on the baseline maple-sim architecture. (The only `FuelSim.java` on this machine is 6328's, under reference dirs.)

### Next up
- ~~Approve & execute `PLAN.md` Phase 1+2~~ — done 2026-07-17, see Done above (commit `d6456a4`).
- ~~Sim-verify the 2026-07-17 auto IMU-mode fix~~ — done 2026-07-17: `PLAN.md` Phase 3 Test A (`RobotLifecycleTest`), see Done above. The mode-*switch* is proven; the pose-*correction* side is explicitly not (see the "honest non-result" note in Done).
- ~~⚠️ `SKILLS/` scripts of unknown origin~~ — resolved 2026-07-17: the user confirmed the `SKILLS/` directory is intentional team tooling. Both scripts were then rewritten from scratch (the original `parse_akit_log.py` printed canned fabricated metrics — including `"IMU Fusion Mode 4 detected"` — without reading a single byte of the log; the original `run_headless_sim.py` had a decorative `-Djava.awt.headless=true` flag, no `JAVA_HOME` handling, a Windows-broken `./gradlew` invocation, an un-drained stderr pipe, a 12 s timeout shorter than a Gradle build, and a `terminate()` that leaves the Gradle daemon + sim JVM alive on Windows). See the Advanced Agent Verification Loop section below for what the rewrites do and what their PASS actually proves.
- **`PLAN.md` Phase 3 Test B (EJECTING) — not started, concrete blocker identified:** `Intake.setRoller()` (`Intake.java:102-108`) doesn't log its commanded state the way `Shooter.setAgitator()`/`indexControl()` already do (`"Agitator State"`/`"Indexer State"` outputs) — so there's currently nothing in the AdvantageKit log to assert EJECT-vs-STOP against for a `RobotLifecycleTest`-style proof. Fix is small (mirror the existing `Shooter` pattern, one `Logger.recordOutput` call in `setRoller()`) but is its own task, not folded into Test A's plan. Once that lands, a Test B can extend the pattern established by `RobotLifecycleTest`: schedule `superstructure.ejectCmd()`, step ticks via `SimHooks.stepTiming()`, assert intake roller/indexer/agitator states from the log outputs; on cancel, assert `STOWED` commands `Roller.STOP`. Interim manual check unchanged: `./gradlew simulateJava`, hold B on the sim controller (port 3).
- **`RobotLifecycleTest` teardown hardening (Minor, from final review, deferred not fixed):** `teardown()` (`RobotLifecycleTest.java:43-49`) has no `try`/`finally` and doesn't check `robotThread.join(1000)`'s result — if the join times out, `robot.close()`/`SimHooks.resumeTiming()` may run on a still-alive thread or get skipped. Harmless today (single method, single class, `forkEvery=1` isolates the JVM anyway) but fold this in the same session a second test class gets added to this file/package — that's also the point to document the "must use `startCompetition()`, not direct lifecycle calls" lesson somewhere more durable than this test's Javadoc (`build.gradle`'s `test{}` comment is the natural spot, next to the existing `forkEvery` explanation).
- **Decide the NamedCommands migration** — `"Intake Start Sequence"` still uses the legacy inline `Commands.parallel(intake.intakeJamReverse(), …agitator IN)` in `RobotContainer`, deliberately: swapping it to `superstructure.intakeCmd()` would silently drop jam-reverse stall protection from autos (the `INTAKING` state drives the roller directly, no jam handling). Options: migrate jam detection into `Intake` as mechanism-level protection (1678-style, preferred) or into the state machine. **Don't swap the registration until jam handling has a new home.**
- Real-robot measurement pass for `Constants.java`/`settings.json` placeholders (see Backlog). The CANcoder boot barrier is in (see Done) — first boot with wheels aligned straight answers the FL/BR offset question; procedure in the vault, `Swerve Offsets & Kinematics.md`.
- **Architectural notes from the 2026-07-16 verification scan:**
  - `RobotContainer.java` and `Telemetry.java` import `com.ctre.*` (`SwerveRequest`, `SwerveDriveState`, `SignalLogger`). This is the *blast radius* of the sanctioned `CommandSwerveDrivetrain` exception — its public API is vendor-typed — not a new Rule 1 leak. Accepted; wrapping would mean wrapping the drivetrain itself, which we decided against.
  - `Superstructure.shootCmd()` declares its requirement (`this`); `shootingSequence()` and `Intake.agitatePivot()` declare **no scheduler requirements**, and `Superstructure.periodic()` writes mechanism setpoints without requirements. Safe today by binding discipline, not enforcement. Trigger to revisit: first observed "two writers, same tick" conflict. Full analysis in the vault Deep Dive, Part II §6.1.
- **Reference-pattern adoption queue** (sized for us, priority-ordered) lives in the vault: `Projects/Elite Reference Architecture Deep Dive.md` §5 + §8. Top items: loop phase split + LoggedTracer, CANcoder boot barrier, config-retry wrapper, cached-signal audit of `IntakeIOReal`/`ShooterIOReal`.

### Backlog
- **Static Calibrations Needed:**
  - Correct the matching front-left / back-right CANcoder offsets (both `0.066650390625` — almost certainly a calibration paste; re-zero BR in Tuner X and compare). *Unverified hypothesis — confirm on the real robot before editing.*
  - Measure the real robot's mass, MOI, wheel-COF, and bumper footprint to replace the remaining `TODO` placeholders in `Constants.java` and `settings.json` (`robotMass`, `robotMOI`, `wheelCOF`, `robotWidth`/`robotLength`, bumper offsets).
  - Audit the high flywheel feedforward `kA = 50` against `kV = 0.215` (~230×; not a plausible flywheel value). *Unverified.*
  - Confirm on the real robot whether Intake pivot/roller and Shooter shoot/index are Kraken X60 or Falcon 500 (both real motors are plain `TalonFX` in `*IOReal.java`, so it's unconfirmable from code) — updates `IntakeIOSim.java`/`ShooterIOSim.java`.

Full detail and evidence for every item above lives in the vault: `Best Practices & Upgrades.md`.

## 📝 The `/tldr` Exit Protocol (End of Session Action)
When the user invokes `/tldr`, immediately:
1. Update the **Current Task State** section of this local `CLAUDE.md` with what was completed during the session.
2. Compile a structured summary of today's code changes, dynamic fixes, and peer-benchmarked discoveries.
3. Write that summary as a new markdown note in `C:\Users\xdm\6767 frc bible\Projects\`, ensuring all newly established parameters or hardware quirks are synced back to the central master brain.

See `.claude/commands/tldr.md` for the full command definition.

## 🔁 Advanced Agent Verification Loop
*Adopted 2026-07-17 after the SKILLS/ audit. "Caveman debugging" — eyeballing code, or trusting a script's printed "SUCCESS" without knowing what it measured — is banned outright.*

**Mandate: every agent workflow that modifies robot code MUST pass gate 1 and gate 2 (`python SKILLS/run_headless_sim.py`; use `python3` on POSIX shells) before claiming completion.** Gate 3 runs whenever a `.wpilog` exists for the claim being verified. A skipped gate means the claim is reported UNVERIFIED — no exceptions.

### The lifecycle (run in order; stop at the first failing gate)
1. **Static gate:** `./gradlew compileJava` exits 0 (with `JAVA_HOME` → WPILib JDK 17, see Build & Test Commands). Never claim a change works before this passes.
2. **Runtime launch gate:** `python SKILLS/run_headless_sim.py`. Launches `gradlew simulateJava -Pheadless=true --no-daemon` (build.gradle disables the sim GUI when `-Pheadless` is set), sets `JAVA_HOME` itself, waits up to 420 s for `********** Robot program starting **********`, then lets the sim run a 12 s settle window and kills the full process tree (`taskkill /T` on Windows).
   - **A PASS proves:** the build compiled, every subsystem/Superstructure singleton constructed without throwing, and disabled-mode periodic loops ran clean.
   - **A PASS does NOT prove:** any command or state-machine *behavior*. The headless sim boots **disabled with no driver station attached**, so the scheduler never runs `intakeCmd()`/`ejectCmd()`/`shootCmd()`. Behavior claims require a JUnit test driving `DriverStationSim`/`SimHooks`, or an interactive `simulateJava` session with the GUI.
2.5. **Behavior gate:** `./gradlew test`. Runs the JUnit 5 suite under `src/test/java` — currently
   `RobotLifecycleTest`, which drives the real `Robot.startCompetition()` loop via
   `DriverStationSim`/`SimHooks` through disabled → autonomous and asserts the `Vision/IMUMode`
   1→4 transition. Unlike gate 2, this exercises actual mode-transition behavior, not just
   launch/construction. A PASS here plus a parsed `.wpilog` (gate 3) is what "verified" means for
   this specific fix — see `docs/superpowers/plans/2026-07-17-phase3-imu-mode-junit-test.md`.
3. **Evidence gate:** `python SKILLS/parse_akit_log.py <file.wpilog>` — a real WPILOG v1.0 binary parser (entry table, record counts, `--dump`, and `--pose-entry`/`--jump-threshold` frame-to-frame pose-jump analysis for `struct:Pose2d`/`double[3]` entries). Every number it prints is computed from the log bytes. Hardened 2026-07-17 against three spec edge cases verified by synthetic-log self-test: records are sorted by timestamp before analysis (the spec does not guarantee order), entry-ID reuse after a Finish record no longer drops the earlier entry's data, and all standard array types (`boolean[]`/`int64[]`/`float[]`/`string[]`/`double[]`) decode.
   - **Evidence path (closed 2026-07-17):** `Robot.java`'s SIM case now adds a `WPILOGWriter("logs")`
     alongside `NT4Publisher`, and `Vision.periodic()` now logs `Vision/IMUMode`/`Vision/IMUAssistAlpha`
     every tick. A `./gradlew test` run (gate 2.5) produces a real `.wpilog` under `logs/` that
     `--dump "/RealOutputs/Vision/IMUMode"` confirms transitions 1→4 at the disabled→autonomous
     boundary — verified from log bytes, not memory. See
     `docs/superpowers/plans/2026-07-17-phase3-imu-mode-junit-test.md` Task 3 for the full evidence run.

### Honesty rules (non-negotiable)
- Verification scripts must **never print canned or hardcoded metrics**. The pre-2026-07-17 `parse_akit_log.py` fabricated `"IMU Fusion Mode 4 detected"` and `"Pose Stability: STABLE"` unconditionally — treat any historical output from it as fiction.
- A gate that cannot run is reported as **UNVERIFIED**, never as passed. Partial verification states exactly what was and wasn't exercised.
- Success claims quote the actual command exit code / output, not a paraphrase.
