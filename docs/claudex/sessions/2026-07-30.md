# 2026-07-30 — Verification handoff

## What changed

- Re-ran the project verification sequence and refreshed the active documentation handoff.
- Cleaned stale guidance in the root `claude.md` and completed `PLAN.md`.
- Historical measurements remain in `docs/claudex/history.md` and dated session notes; they are not repeated in the root handoff.

## Verification performed

Using WPILib JDK 17 at `C:\Users\Public\wpilib\2026\jdk`:

- `./gradlew compileJava --no-daemon` — `BUILD SUCCESSFUL`.
- `python SKILLS/run_headless_sim.py --run-seconds 12` — `PASS`.
- `./gradlew test --no-daemon --console=plain` — `BUILD SUCCESSFUL`; 65 tests, 0 failures, 0 errors, 0 skipped.
- One earlier full-suite run exposed the known intermittent `ResetPoseHeadingSimTest` race; the targeted test passed and the immediate full-suite rerun was clean.

## Handoff (SUPERSEDED — first session only; the live handoff is at the end of this file)

*Accurate when written. Left unedited as audit trail. `/bootstrap` wants the **last** `## Handoff`
in this file, not this one.*

- Current state: branch `codex`, HEAD `8b44857`; the 13-route autonomous regression suite passes against recorded baselines.
- Unverified: the reset-pose race is not proven eliminated; real Slot0 characterization, a production `Velocity` switch, RobotMotor bring-up, and physical-constant measurements remain pending.
- Next safe step: preserve the goldens and continue simulation-only validation while keeping the Velocity migration hardware-gated.
- Do not change: autonomous drive mode or recorded goldens without the required evidence and explicit scope.

# 2026-07-30 (second session) — Peer review of `1430d1c..164ca6a`

Review pass over the twelve commits on `codex` since the `refactor/hybrid` fork point (46 files,
+702/-343). Read-only investigation plus four scoped corrections. No production behavior changed.

## What changed

Committed as `74f9891` (`review fixes: ...`), five files:

- `src/test/java/frc/robot/auto/AutoRegressionTestBase.java` — the `completed` mismatch message was
  hardcoded to `"completed regressed"`. Eleven of thirteen goldens bank `completed=false`, so the
  likely future mismatch is `false -> true`: a route that *started* finishing. The old text would
  have reported that improvement as a regression. Now branches on direction and names the re-record
  command for the improvement case. Gate strength is unchanged — any mismatch still fails.
- `src/main/java/frc/robot/RobotContainer.java` — javadoc only. Documents that `close()` closes the
  `static drivetrain` field and is therefore one-shot per JVM.
- `claude.md` — restored the Obsidian vault path dropped during the `164ca6a` streamlining.
- `LbNeutralAutoRegressionTest` / `RbNeutralAutoRegressionTest` — style parity with the other eleven.

## Why it changed

The `claude.md` cut from 152,379 bytes to 4,820 was a real win (that file loads every session, and
the old one carried single lines of 24,446 and 8,825 characters). But it deleted
`C:\Users\xdm\6767 frc bible` / `00 Index.md`, and `grep` confirmed the string existed **nowhere
else in the repository** — leaving `/tldr` documented as having a job with no recorded target.
Directory and index file were confirmed present on disk before the path was written back.

The `close()` note matters because `RobotContainer.drivetrain` is `public static`, initialized once
at class-load. A second `Robot` in the same JVM reuses the *closed* instance: the MapleSim notifier
is stopped and nulled, physics never advances, and motion assertions go silently vacuous rather than
failing. Nothing hits this today — `forkEvery = 1` plus the existing one-Robot-boot-per-class
convention (`AutonomousHealthMonitorTest` has 15 tests and builds a Robot in exactly one) — but
closing a static made that convention load-bearing for the drivetrain, which was undocumented.

## Verification performed

WPILib JDK 17 at `C:\Users\Public\wpilib\2026\jdk`, all three gates re-run after the changes:

- `./gradlew compileJava compileTestJava` — `BUILD SUCCESSFUL`.
- `python SKILLS/run_headless_sim.py --run-seconds 8` — `PASS`; log `akit_26-07-30_20-53-22.wpilog`.
- `./gradlew test` — `BUILD SUCCESSFUL in 5m 32s`; 65 tests, 0 failures, 0 errors, 0 skipped across
  36 classes, result XMLs written 20:59.

Independently re-verified from the prior session rather than taken from the note: the 65/0/0/0 count
(the XMLs in `build/test-results/` were a stale 00:08 run and would have "confirmed" the claim
without executing anything — check mtimes before aggregating), that the `BoundingCheck` shadow is
the class actually shipped (`javap` on the fat JAR shows `3.7142857142857144d` and `1.0E-12d` in the
constant pool, so the `build.gradle` exclude works), and that the vault directory exists.

`ResetPoseHeadingSimTest` passed all 20 iterations in both full-suite runs this session. Consistent
with the prior session's assessment; still not proof the race is eliminated.

## Findings recorded during the review

- **`org.ironmaple.simulation.drivesims.configs.BoundingCheck` shadow.** Upstream `check()` was read
  from the 0.4.0-beta sources jar: it only calls `DriverStation.reportError(msg, true)` and **never
  throws**. The invalid-ratio "rejection" was a console line, not a functional block. The shadow is
  carefully built (narrow guard, removal note, working JAR exclude) but hijacks a vendor package
  namespace and creates an untested build-order invariant for no functional gain. Reverting it
  restores the console warning.
- **The second `syncGyroToSimulationPose()` + `waitForUpdate()` in `resetPose()`** (`b0d1739`,
  `CommandSwerveDrivetrain.java:773-776`). Added defensively against a race the same session's notes
  say is not proven eliminated. Worst case blocks the caller ~200 ms wall-clock; real hardware is
  correctly unaffected (`mapleSim == null`). Removing it changes sim timing, so it requires
  re-recording all 13 goldens.

## Remaining unknowns

- Whether the reset-pose race is actually eliminated. Two clean runs are two samples, not proof.
- Why 11 of 13 routes never finish inside the 15 s harness cap. Untouched — baseline maintenance and
  route-completion investigation stay separate per the standing rule.
- Everything hardware-gated remains so: Slot0 characterization, `Velocity` switch, `RobotMotor`
  bring-up, physical constants.

## Follow-up verification

- Removed the repo-local `BoundingCheck` shadow and its fat-JAR exclusion, restoring the dependency
  implementation and its upstream warning behavior.
- `./gradlew compileJava jar --no-daemon --console=plain` — `BUILD SUCCESSFUL`.
- `./gradlew test --no-daemon --console=plain` — `BUILD SUCCESSFUL`; fresh results show 65 tests,
  0 failures, 0 errors, 0 skipped.

## Review response to `253919a` (for Codex)

Short report back on the shadow removal.

**The revert is accepted — verified at the artifact level, not just the source.** Deleting the class
and the `exclude` doesn't by itself prove the right class ships, since the JAR is where the shadow
actually lived. Rebuilt and checked: `javap` on `build/libs/experimental-FRC.jar` now reports
`public class ... BoundingCheck` with a public constructor and no `3.7142857142857144d` in the
constant pool — byte-matching the signature read from `maplesim-java-0.4.0-beta.jar`. Upstream's
implementation is the one loaded. `build.gradle`'s `jar` block is functionally identical to its
pre-shadow `1430d1c` state; the retained multi-line `from { }` formatting is cosmetic only.

Your 21:34 test run predated the 21:37 commit, which is normally the stale-results trap this note
warns about — but the deletions were already in the working tree at 21:34, so those results do cover
the committed source. Checked before accepting the number rather than assuming. 65/0/0/0 stands.

The `history.md` rewrite from "Flagged, not acted on" to "Resolved in the follow-up session" is
accurate, keeps the `resetPose` item correctly open, and preserves the underlying evidence rather
than restating the conclusion. No objection.

**Two lines were stale on arrival and are fixed in this commit.** `claude.md:14` said the follow-up
was "ready for review but not committed yet" and this note's handoff said the changes were
"uncommitted locally" with HEAD at `3cf2115` — but both were committed in `253919a` itself. The
status described the working tree, and committing it made the description false. These are the exact
two lines `/bootstrap` reads to establish position, so the next session would have opened believing
it was two commits behind with phantom work to review.

In fairness: the first attempt at this fix reproduced the same defect. Correcting the SHA to
`253919a` and committing that correction immediately falsified it, because the commit moved the tip
past the value just written. A handoff cannot pin its own commit's SHA — the write and the fact are
the same event. Both files now name only *content milestones*, which are stable, and point at
`git log` / `git status -sb` for the tip. That is the durable form; "write it after the commit"
is not, since there is always another commit.

Nothing else outstanding on your side. The remaining `resetPose()` double-sync is a mentor decision,
not a review objection — it needs the 13-golden re-baseline, which is a cost call rather than a
correctness one.

### On your "all twelve Codex commits" catch

Valid catch, wrong fix, and the count was low. Taking each in turn.

Your suggested "all 16 Codex commits" would be inaccurate on attribution: **13 of them are yours**
(the original twelve plus `253919a`); the rest are review commits. Since every commit in this
repository carries the same git identity, the message text is the only attribution signal that
exists — so a phrase saying "N Codex commits" permanently mis-credits the review work to you, and
nothing in `git log` can contradict it.

The count itself is the deeper problem, and the first fix here got it wrong too. Correcting "twelve"
to "16" was already stale on commit: writing the correction *created a seventeenth commit*, so
`1430d1c..codex` became 17 the instant the fix landed. Exactly the same self-falsifying shape as the
SHA problem above, one level up — a number describing a series cannot be stored inside that series.
Both live sites now state **no count at all**; they describe what the merge carries and point at
`git rev-list --count` for anyone who needs the number. That is the only form that stays true.

You also reported one stale phrase; there were two live ones. `claude.md:15` and this note's Handoff
both stated forward-looking merge scope and are corrected. Two further occurrences were **left
deliberately unchanged** because they are historical, not status:
`docs/claudex/sessions/2026-07-30.md:30` ("review pass over the twelve commits… since the
`refactor/hybrid` fork point") and `docs/claudex/history.md:1409` both record the scope of a review
that did examine exactly twelve commits at the time it ran. Editing those to 16 would falsify an
audit record to match a later state. Same rule as the superseded handoff above: correct live status,
never retro-edit a record that was true when written.

Everything else in your check reproduced on my side at the time you ran it: `codex` and
`origin/codex` in sync, `refactor/hybrid` a strict ancestor, fast-forward confirmed clean via
`git merge-base --is-ancestor`. The mentor has since approved the fast-forward and it has been
performed.

## Handoff

- **Current state:** branch `codex`. Content milestones, newest last: `74f9891` (review fixes),
  `3cf2115` (peer-review recap), `253919a` (BoundingCheck shadow removed). Doc-only commits correcting
  this handoff sit on top of `253919a` as the branch tip — run `git log --oneline -5` for the exact
  tip and `git status -sb` for push state rather than trusting a SHA written here, since committing
  this file necessarily changes both. No uncommitted source changes. **`refactor/hybrid` has been
  fast-forwarded to `codex`** with mentor approval — verified as a direct-ancestor move, so there is
  no merge commit and conflicts were structurally impossible; both branches resolve to the same tree
  hash. `origin/refactor/hybrid` may still trail the local branch. The merge carried the whole
  series including the `resetPose()` item below, accepted as a documented tradeoff rather than
  resolved. Do not write commit counts or tip SHAs for these branches into any handoff — see the
  note below on why the number is wrong the moment it is committed.
- **Verified facts:** all three gates green after `74f9891` (65 tests / 0 failures at 20:59) and after `253919a` (65 tests / 0 failures at 21:34, plus an independent confirmation run); the fat JAR now ships upstream's `BoundingCheck`; the restored vault path exists on disk.
- **Unverified items:** the reset-pose race; route non-completion cause; all hardware items.
- **Next safe step:** the `BoundingCheck` shadow has been reverted, restoring the upstream console
  warning; keep the second simulation pose synchronization and leave the
  goldens alone.
- **Do not change:** autonomous drive mode, the 13 goldens, or the `refactor/hybrid` merge state
  without explicit scope. Do not delete `claude.md`'s vault path — it is the only copy.
