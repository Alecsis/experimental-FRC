# 2026-07-28 — Drive Slot0/autonomous-control-path audit trail + Velocity migration plan (thirty-first session)

## What changed

Four new documentation-only files, all untracked, none committed:

1. **`docs/Drive_Slot0_Retuning_Audit.md`** — the mentor's requested "complete SysId/Slot0 retuning audit."
   Decompiled the actual shipped `wpiapi-java-26.1.3-sources.jar` (found in the local Gradle cache, matched exactly
   against `vendordeps/Phoenix6-26.1.3.json`) to confirm, directly from CTRE's own source doc comments, that drive
   `Slot0` gains operate on raw motor-rotor rotations (`FeedbackConfigs.RotorToSensorRatio`/`SensorToMechanismRatio`
   hardcoded to `1.0` for the drive motor) while steer `Slot0` gains operate on post-gear-ratio azimuth rotations —
   and that this asymmetry makes the existing `docs/SysId_Characterization_Checklist.md`'s own `12/kV` vs.
   `kSpeedAt12Volts` sanity check unit-inconsistent as written. Provided the corrected formula. Flagged a stale,
   actively misleading comment at `RobotContainer.java:52` (says "open-loop," code sets `DriveRequestType.Velocity`).

2. **`docs/Autonomous_Control_Path_Audit.md`** — a follow-up the mentor requested to independently re-verify the
   first audit's most consequential claim with a complete file:line call graph. Decompiled both
   `wpiapi-java-26.1.3-sources.jar` and `PathplannerLib-java-2026.1.2-sources.jar` and traced
   `AutoBuilder.configure()` → `FollowPathCommand.execute()` → `PPHolonomicDriveController.calculateRobotRelativeSpeeds()`
   → `CommandSwerveDrivetrain`'s output lambda → `ApplyRobotSpeeds.applyNative()` → a named JNI entry point, with an
   exact citation at every hop. Confirmed directly (not secondhand) that `PPHolonomicDriveController` sums
   feedforward and feedback with zero clamping, that `ApplyRobotSpeeds.DriveRequestType` defaults to
   `OpenLoopVoltage` and is never overridden by this repo's `configureAutoBuilder()`, and that `DriveFeedforwards` is
   a precomputed record from the trajectory generator, structurally independent of any PID controller. Flagged one
   genuine unknown (whether wheel-force feedforward has any effect under `OpenLoopVoltage` — native/JNI boundary)
   with a concrete, described experiment to resolve it later.

3. **`docs/Autonomous_Drive_Architecture_Recommendation.md`** — a third mentor-requested follow-up: a fair,
   evidence-based comparison of `OpenLoopVoltage` vs. `Velocity` across six dimensions, plus research into what CTRE
   and other teams' shipped code actually do. Read CTRE's own official `Phoenix6-Examples` reference
   (`temp_reference/`) and found it matches this repo's `OpenLoopVoltage` default exactly — but also calls
   `ChassisSpeeds.discretize()`, which this repo is missing. Read Team 254, Team 1678, and Team 6328's shipped
   drivetrain code (`temp_reference/`, `frc-steal-from-the-best/`) and found all three independently choose
   closed-loop, feedback-driven control for autonomous/precision-tracking contexts, with zero counterexamples in the
   sample. Also corrected a likely misconception along the way: neither control mode addresses ground-relative wheel
   slip — that's governed by `kSlipCurrent`, orthogonal to `DriveRequestType`. Concluded with a single recommendation:
   switch to `Velocity`, conditional on completing SysId characterization first — explicitly not a claim that the
   switch alone fixes the already-documented autonomous tracking error, since the confirmed-dominant lever (chassis
   PID) is independent of this choice.

4. **`docs/superpowers/plans/2026-07-28-autonomous-velocity-migration.md`** — a full phased implementation plan
   (via the `writing-plans` skill), covering the mentor's six specified phases (0: discretization fix; 1: SysId;
   2: Slot0 tuning; 3: switch to `Velocity`; 4: before/after comparison; 5: keep/revert decision), each with
   expected behavior, TDD task breakdowns (Phases 0/3, with real code), procedural steps (Phases 1/2), regression
   tests, success criteria, and a rollback plan. Phase 4's design reuses the existing auto-regression harness almost
   entirely (only current-draw capture needed new code, `WpilogCurrentDrawReader`, mirroring the existing
   `WpilogTrajectoryErrorReader` exactly) and explicitly calls out a methodological trap: comparing post-switch
   results against the *original* pre-Phase-0 golden would confound three changes at once, so the plan requires
   capturing an isolated before/after snapshot right at the Phase 3 boundary.

   **Mid-session revision, per explicit mentor direction:** the mentor first asked to implement and validate the
   SysId workflow in simulation only (to prove command/logging/data-collection/integration, not to produce real
   gains) — this was started (the `test-driven-development` skill was loaded, WPILib `DriverStationSim` API
   verification was in progress) but interrupted before any code was written, and redirected to plan-only: just
   note the sim-first approach in the plan document. Phase 1 was rewritten accordingly — it now runs entirely in
   simulation, produces no production values, and opens with an explicit `⚠️ TODO` block stating sim-derived
   `kS`/`kV`/`kA` must never be pasted into `TunerConstants.driveGains` and that Phase 2 is not complete until real
   hardware characterization happens. Phase 2 gained a matching precondition block carrying that TODO forward and
   blocking its `Slot0`-gain task until the physical run occurs. The plan's top-level Architecture summary and
   Global Constraints section were updated to match, so the document doesn't contradict itself between sections.

**Zero production code was touched this entire session. Zero commits were made.**

## Why it changed

Continues directly from `CLAUDE.md`'s "corrected fix order" (SysId → drive `Slot0` → chassis PID), the standing
next-priority item on the robot-code stack since the VT-input-relay migration arc closed at the end of the thirtieth
session. The mentor's first request this session — "a complete SysId/Slot0 retuning audit" — surfaced a fact that
fix order didn't account for: autonomous path-following runs `OpenLoopVoltage`, which never consults `Slot0` at all,
meaning that established fix order was never going to address the specific tracking-error problem it was written
for. Each subsequent mentor request (re-verify with a full call graph; compare the two architectures fairly; design
a migration plan; scope Phase 1 to simulation) was a deliberate step of confirming that finding thoroughly enough to
act on before committing engineering time to a real architecture change.

## Verification performed

No `./gradlew` gates apply — no `src/` Java file changed this session. Verification here means source-level
verification of every claim made: decompiled and read the actual shipped `wpiapi-java-26.1.3-sources.jar` and
`PathplannerLib-java-2026.1.2-sources.jar` from the local Gradle module cache (both version-matched exactly against
this repo's `vendordeps/`), rather than relying on public docs prose or secondhand citation from prior sessions.
Cross-referenced CTRE's own official example and three elite teams' shipped source for the architecture comparison.
`git status --short` confirmed clean before and after every document written this session — final state: four new
untracked files, nothing else changed, nothing committed.

## Remaining unknowns

- Whether PathPlanner's per-module wheel-force feedforward has any effect under `OpenLoopVoltage` — the conversion
  happens inside CTRE's native/JNI layer (`SwerveJNI.JNI_SetControl_ApplyRobotSpeeds`), not visible from decompiled
  Java source. A concrete A/B telemetry experiment to resolve this is described in
  `docs/Autonomous_Control_Path_Audit.md` §5a but has not been run.
- Whether to actually keep `OpenLoopVoltage` or switch to `Velocity` — `docs/Autonomous_Drive_Architecture_
  Recommendation.md` concludes "switch, conditional on SysId first" as evidence-based guidance, but this is not a
  made decision; the migration plan's Phase 5 is where the mentor decides, informed by Phase 4's real comparison
  data, which doesn't exist yet since no phase of the plan has been implemented.
- Phase 1's sim-only SysId workflow validation (Task 1.1/1.2) is fully scoped in the plan but not implemented — no
  test code exists yet for it.

## Next safe action

Either of two unblocked, hardware-independent starting points, both fully scoped with exact code in the plan doc:
- **Phase 0**: add the missing `ChassisSpeeds.discretize()` call (TDD, lowest-risk item identified across all three
  audits — two independent sources, CTRE's own example and Team 6328, both already do this).
- **Phase 1**: implement the sim-only SysId workflow validation as directed this session.

Do not skip ahead to Phase 2 (blocked on a physical-robot characterization run that hasn't happened) or Phase 3
(requires explicit mentor sign-off per the plan's own non-defaultable-decision-point rule).

## Update — Phase 0 Task 0.1 implemented (later, same day)

### What changed

Implemented Task 0.1 of `docs/superpowers/plans/2026-07-28-autonomous-velocity-migration.md`: extracted the
autonomous speed-preparation pipeline into a new package-private `CommandSwerveDrivetrain.prepareAutoSpeeds
(ChassisSpeeds)`, which now calls `ChassisSpeeds.discretize()` before the existing `sanitizeAutoSpeeds()` bound.
`configureAutoBuilder()`'s lambda now calls `prepareAutoSpeeds(speeds)` instead of `sanitizeAutoSpeeds(speeds)`
directly. Added the plan's two tests to `CommandSwerveDrivetrainSanitizeSpeedsTest.java` (composition-order pin,
no-op-discretize guard) via TDD — watched both fail on `cannot find symbol: method prepareAutoSpeeds`, then
implemented. Marked Task 0.1's six checkboxes done in the plan doc. Committed as `309a89b` ("Extract
prepareAutoSpeeds helper for autonomous velocity migration" — commit message per explicit instruction this task,
not the plan's own drafted message). No `DriveRequestType` change, no later-phase work touched. Zero files outside
`CommandSwerveDrivetrain.java`, its test, and the plan doc were touched.

### Why it changed

The first of the two unblocked, hardware-independent starting points identified at the end of the audit trail above.
Closes the discretization gap flagged in `docs/Path_Following_Tuning_Readiness_Audit.md` §4 and confirmed against
two independent elite-team implementations, while leaving `OpenLoopVoltage`/chassis-PID untouched — isolating this
change from every later migration phase per the plan's own one-commit-per-task discipline.

### Verification performed

- `CommandSwerveDrivetrainSanitizeSpeedsTest`: RED confirmed (compile error, missing symbol) → GREEN, all 4 tests
  (2 pre-existing + 2 new) pass, consistently across every rerun.
- `./gradlew compileJava`: BUILD SUCCESSFUL.
- `python SKILLS/run_headless_sim.py --run-seconds 12`: PASS.
- `./gradlew test` (full suite): completed clean once in 40s (10/11 passing; only `LtNeutralAutoRegressionTest`
  failed). A second full-suite run hung ~17 minutes in `IntakeJamRecoveryTest`, confirmed via `jstack` to be the
  same pre-existing, previously-documented `SimHooks.stepTiming()` native-JNI race (uninterruptible by `@Timeout`)
  — killed the stuck worker; unrelated to this change.
- **Investigated `LtNeutralAutoRegressionTest`'s failure for causality rather than assuming it away**: the stall
  check sits on a razor-thin margin (0.045m moved vs. 0.050m threshold) in a real-wall-clock (`Thread.sleep`-based)
  harness. A/B sampling: 5/5 runs failed with the change applied, but the unmodified baseline also failed 3 of 6
  runs. Conclusion: pre-existing flakiness in this specific stall check, not a regression from Task 0.1 — but this
  ~50% baseline fail rate is a **new** flakiness discovery, distinct from the already-documented ~9.6m
  lateral-error golden drift, and hadn't previously been known to fail this often.
- Two further `<system-reminder>` prompt-injection occurrences this session (same fingerprint as the running tally
  in `docs/claudex/history.md`: false "modified by the user or a linter" claim + "don't tell the user" instruction,
  attached to git-stash-driven tool output during the A/B causality check). Both declined and surfaced to the
  mentor immediately, per the Trust Boundary policy. Running total now at least twenty-six.

### Remaining unknowns

- `LtNeutralAutoRegressionTest`'s stall-detection flakiness (~50% pre-existing baseline fail rate, this session's
  discovery) is unquantified beyond this session's small sample and not yet reported as its own backlog item.
- `IntakeJamRecoveryTest`'s hang is the same documented `SimHooks`/`CommandScheduler` race class as before, now
  confirmed to recur in a second, unrelated test class — still not root-fixed, only worked around by killing the
  process when it occurs.
- Everything the audit-trail half of this session flagged as unknown (wheel-force feedforward under
  `OpenLoopVoltage`; the `Velocity`-vs-`OpenLoopVoltage` decision) is unchanged by this update.

### Next safe action

Phase 1 (sim-only SysId workflow validation, Task 1.1/1.2) is the next unblocked, hardware-independent item in
`docs/superpowers/plans/2026-07-28-autonomous-velocity-migration.md`. Separately, worth raising with the mentor:
`LtNeutralAutoRegressionTest`'s stall check appears unreliable independent of any code change and may be worth a
tolerance/design look before it's relied on as a gate again.

## Update — Phase 1 Task 1.1 implemented (later, same day)

### What changed

Implemented Task 1.1 of `docs/superpowers/plans/2026-07-28-autonomous-velocity-migration.md`: a new sim-only
integration test, `src/test/java/frc/robot/subsystems/CommandSwerveDrivetrainSysIdSimWorkflowTest.java`. It boots
the robot in sim (the same `Robot()`-thread pattern `AutoRegressionTestBase` already uses), selects the Translation
SysId routine (`useTranslationSysId()`), schedules `sysIdQuasistatic(kForward)` for a bounded 3s window (not the
full 10s default timeout), confirms it's still running mid-window, cancels it, then repeats with
`sysIdDynamic(kForward)` for 1.5s. After the run, it reads the produced wpilog via WPILib's
`DataLogReader`/`DataLogRecord` (the same library `WpilogTrajectoryErrorReader` uses) and asserts
`Drive/AppliedVoltsPerModule` shows more than 10 samples, a max-abs value over 0.5V, and more than 3 distinct
values — proving the signal actually ramped/stepped, not that it sat at a constant. **No production code was
touched** — every method the test calls already existed and was already wired; this is a new test file only. Also
updated Task 1.1's section in the plan doc to record what was implemented, replacing its "not yet implemented"
prose with checked steps.

### Why it changed

The next unblocked, hardware-independent item identified at the end of the audit-trail/Phase-0 work earlier today.
Per the mentor's explicit direction this session, this proves the already-wired SysId command lifecycle, telemetry
capture, and analysis-pipeline readability all work correctly in sim *before* ever touching the physical robot —
without producing or asserting any value that could be mistaken for real characterization data (per the plan's own
Phase 1 TODO, carried forward unchanged).

### Verification performed

- New test: 3/3 consecutive passing runs (`./gradlew test --tests "...CommandSwerveDrivetrainSysIdSimWorkflowTest"`).
- `./gradlew compileJava`: BUILD SUCCESSFUL.
- `python SKILLS/run_headless_sim.py --run-seconds 12`: PASS.
- Full `./gradlew test` (all 7 test classes / 12 tests in this repo): 11/12 passed. The one failure —
  `LtNeutralAutoRegressionTest`'s stall check, "moved 0.047m over the preceding 0.98s (threshold 0.050m)" — is the
  same pre-existing razor-thin-margin flakiness class documented in this file's own Task 0.1 update above (there it
  was 0.045m vs 0.050m; here 0.047m vs 0.050m). This task touched zero production code, so there is no plausible
  causal path from this change to that auto's behavior — not re-litigated as a regression.
- Gate 3 (`python SKILLS/parse_akit_log.py`), run independently against the new test's own freshly-produced wpilog:
  confirmed by direct inspection of `/RealOutputs/Drive/AppliedVoltsPerModule` samples — a clean, textbook 1V/s
  linear voltage ramp across all four modules in lockstep (e.g. 0.06V at t=0.083s rising to 0.82V at t=0.843s),
  exactly matching `SysIdRoutine.Config`'s default 1V/s quasistatic ramp rate. This independently cross-validates
  the test's own internal assertions using a separate tool, not just the test grading itself.
- **Genuine finding, recorded rather than assumed either way:** Phoenix's `SignalLogger` (the `.hoot` log, per
  `docs/SysId_Characterization_Checklist.md` §3) produces **no file** under this headless JUnit sim harness,
  confirmed consistently across all 3 runs — `SignalLogger.start()` and the `SysIdTranslation_State` callback's
  `writeString()` calls never throw, but nothing lands in `logs/`. Most likely explanation: no real CAN
  bus/CANivore is present in this harness for Phoenix's native logger to attach to. Documented in the plan doc so a
  future session doesn't spend time trying to make headless JUnit sim produce an analyzable `.hoot` file — it
  structurally can't without real CAN hardware.

### Remaining unknowns

- Task 1.2 (compute a placeholder kS/kV from the sim run's data via manual linear regression, to prove the analysis
  leg specifically) is not started, per this session's explicit "Task 1.1 only" instruction.
- Whether Phoenix's hoot-log capture would work under a different sim configuration (e.g. an actual CANivore
  simulator or Phoenix's own simulation CAN backend) is unexplored — this session only established that it doesn't
  happen under the existing JUnit harness as configured.
- Everything flagged unverified earlier today (wheel-force feedforward under `OpenLoopVoltage`; the
  `OpenLoopVoltage`-vs-`Velocity` decision; `LtNeutralAutoRegressionTest`'s true baseline fail rate) is unchanged by
  this update.

### Next safe action

Task 1.2 (sim-derived placeholder kS/kV regression, proving the analysis pipeline specifically) is the next
in-scope item in Phase 1, if wanted. Otherwise Phase 2 remains hard-blocked on physical-robot access regardless.

## Update — Phase 1 Task 1.2 implemented (later, same day, new session)

### What changed

Implemented Task 1.2 of `docs/superpowers/plans/2026-07-28-autonomous-velocity-migration.md`: documentation/analysis
only, no production or test code touched. Re-ran `CommandSwerveDrivetrainSysIdSimWorkflowTest` to produce a fresh
wpilog (`logs/akit_26-07-28_11-56-19.wpilog`, PASSED). A one-off Python script (written to the session scratchpad,
not committed — this task is scoped as documentation/analysis-only) reused `SKILLS/parse_akit_log.py`'s existing
wpilog parser to pair `/RealOutputs/Drive/AppliedVoltsPerModule` (mean abs across 4 modules) against
`/RealOutputs/DriveState/Speeds`'s `vx` by nearest timestamp (all 151 samples matched within 20ms), then fit the
same two-term model SysId's own analyzer uses, `V = kS·sign(vx) + kV·vx`, via ordinary least squares. Result:
`kS = 0.1849 V`, `kV = 1.7293 V·s/m`, R² = 0.9198 over 151 paired samples. Recorded in new
`docs/SysId_Sim_Workflow_Validation.md`, opening with the exact "SIMULATION-DERIVED PLACEHOLDER — DO NOT USE FOR
REAL ROBOT CHARACTERIZATION" warning the plan's Phase 1 TODO requires, repeated verbatim so the doc is self-contained.
Updated Task 1.2's section in the plan doc to record what was implemented (checked steps, replacing its "not yet
implemented" prose). Committed both files as `2ac441a` ("Document simulated SysId workflow validation").

### Why it changed

The next in-scope item in Phase 1 per the prior update's own "Next safe action." Closes the loop on Task 1.1's
telemetry-capture proof by proving the *analysis* leg specifically: that a real (if simulated) wpilog's paired
telemetry supports a well-conditioned linear-regression fit, not just that the file exists and is readable.

### Verification performed

- `git status --short` before and after: only `docs/SysId_Sim_Workflow_Validation.md` (new) and the plan doc
  changed; `TunerConstants.java` and every other production/test file untouched — confirmed directly via `git diff
  --stat`, not assumed from having "only written docs."
- The regression fit itself: R² = 0.9198 over 151 samples is the direct verification that the analysis pipeline
  (wpilog → parse → timestamp-match → least-squares fit) produces a non-degenerate, well-conditioned result rather
  than silently returning garbage — this is Task 1.2's entire success criterion, not a side effect.
- No `./gradlew` gate applies beyond the one test re-run (`CommandSwerveDrivetrainSysIdSimWorkflowTest`, PASSED) —
  no production Java changed this task.

### Remaining unknowns

- Everything flagged unverified in the prior updates today is unchanged: wheel-force feedforward under
  `OpenLoopVoltage`; the `OpenLoopVoltage`-vs-`Velocity` decision (Phase 5, gated on Phase 4 data); the true
  baseline fail rate of `LtNeutralAutoRegressionTest`'s stall check; whether Phoenix hoot-log capture is achievable
  under any sim configuration.
- Phase 1 (Tasks 1.1 and 1.2) is now complete as scoped — no new unknowns introduced by this task itself.

### Next safe action

Phase 1 is done. Phase 2 (real hardware `Slot0` characterization) remains hard-blocked on physical-robot access,
unaffected by this task. No further code-adjacent action is available on this thread until that access exists.

## Handoff

- **Current state:** `refactor/hybrid` @ `2ac441a`, working tree clean. Phase 0 (Task 0.1) and Phase 1 (Tasks 1.1
  and 1.2) are all complete and committed (`309a89b`, `7518244`, `2ac441a`; the four audit/plan docs landed earlier
  in `5eba273`).
- **Unverified items:** whether wheel-force feedforward does anything under `OpenLoopVoltage` (native/JNI boundary,
  experiment described but not run); the `OpenLoopVoltage`-vs-`Velocity` decision itself (Phase 5's job, gated on
  Phase 4 data that doesn't exist yet); the true baseline fail rate of `LtNeutralAutoRegressionTest`'s stall check
  (still only a small sample across today's session); whether Phoenix hoot-log capture is achievable in any sim
  configuration (not explored beyond the existing JUnit harness); the sim-derived placeholder `kS=0.1849`/
  `kV=1.7293` values themselves prove nothing about the real robot — that's the whole point, not a gap to close.
- **Next safe step:** Phase 2 (real hardware `Slot0` characterization) is next in the plan, but requires physical-
  robot access that doesn't currently exist. No other unblocked Phase-1-adjacent work remains. Separately still
  worth raising with the mentor: `LtNeutralAutoRegressionTest`'s stall check appears unreliable independent of any
  code change and may be worth a tolerance/design look before it's relied on as a gate again.
- **Things not to change:** do not paste sim-derived `kS`/`kV`/`kA` into `TunerConstants.driveGains` under any
  circumstance — Task 1.2's own placeholder numbers (`kS=0.1849`, `kV=1.7293`) are explicitly not for that; do not
  implement Phase 3 (the `DriveRequestType.Velocity` switch) without explicit mentor sign-off, per the plan's Global
  Constraints; do not touch Controls Academy or the autonomous-completion-trigger-framework thread — both unrelated
  and untouched this session; do not treat `LtNeutralAutoRegressionTest`'s stall-check failures as a regression from
  any of Task 0.1/1.1/1.2 — investigated each time and attributed to pre-existing flakiness, not these changes; do
  not attempt to make headless JUnit sim produce an analyzable `.hoot` file — confirmed structurally absent from
  this harness, not a bug to chase.
