# 2026-07-23 — VT-input-relay Phase 5 live validation + Phase 4 cleanup + merge (thirtieth session)

## What changed

Closed out the VT-input-relay migration arc that ran through the twenty-fourth–twenty-ninth
sessions. The mentor reported Plan Phase 5's mandatory live-validation checklist passing against a
real Windows Terminal session (all ten items: normal typing, mouse wheel scrollback, Shift+Tab
mode switching, copy/select, multi-line paste with newlines preserved as a single prompt, bracketed
paste, terminal resize, Discord `!reply`, the checkpoint alert→approve/reply→resume flow, VT
console-mode restore on exit — plus no regressions during extended use) and asked to proceed with
Plan Phase 4 cleanup: merge into `refactor/hybrid`, thinner-not-smarter deletions only, preserve
required behavior, full suite green before and after merging, remove only plan-marked-obsolete
legacy code, verify a clean tree.

Before touching code, cross-checked the report against the plan's own text rather than accepting it
at face value, and surfaced two gaps the plan explicitly refuses to default:

1. The report didn't explicitly confirm Task 3.6's deliberate behavior change (local Ctrl+C now
   forwards to Claude as a literal byte instead of killing the supervisor) — the plan requires live
   sign-off for this, not unit tests alone.
2. The legacy `msvcrt`/mouse-translation backend's fate (delete outright vs. keep as a config-flag
   fallback) was left an explicit open decision at Phase 4 kickoff, not a default to assume.

Both closed via `AskUserQuestion`: Ctrl+C confirmed tested and acceptable; legacy backend to be
deleted entirely.

**Phase 4 executed** in `.worktrees/supervisor-vt-input-relay` (branch
`feature/supervisor-vt-input-relay`):

- `claude_supervisor/pty_session.py`: 848→539 lines (**-309, -36%**). Removed the entire legacy
  translation layer per the plan's Phase 0 list (`translate_keystroke`, `_SPECIAL_KEYS`,
  `classify_record`, `extract_wheel_delta`, `translate_wheel_event`, `_enable_mouse_console`,
  `_mouse_loop`, `strip_mouse_tracking`, `_MOUSE_MODE_RE`, `_debug_log`/`_MOUSE_DEBUG_LOG`, six
  ctypes structures/unions, the Win32 mouse/wheel constants, `_input_loop`, `_drain_input`) plus the
  bracketed-paste helpers, which turned out to be dead code even before deletion (`_vt_relay_loop`
  never called them). `_console_vt_plan()`/`_enable_vt_console()` also lost their now-dead `backend`
  parameter — one step beyond the plan's literal letter, justified directly by Task 4.x's own
  "thinner-not-smarter audit" instruction. `_read_loop` is now pure passthrough.
- `PtySession.__init__`/`start()`: dropped `input_backend` param and the `_input_backend`/`_input`/
  `_mouser` attributes; VT console mode and the relay thread are now unconditional.
- `claude_supervisor/config.py`: removed `BehaviorConfig.local_input_backend` and its validator.
- `run.py`: removed the `input_backend=` argument from the `PtySession(...)` construction call.
- Tests: deleted `test_pty_mouse_strip.py`/`test_pty_mouse_wheel.py` (plan-mandated) and
  `test_config.py` (own judgment, confirmed via `git log --follow` that it existed solely for the
  removed field); rewrote `test_pty_input.py` (194 lines/11 tests → ~80 lines/2 tests),
  `test_pty_vt_backend.py` (3→1 tests), `test_pty_console_restore.py` (stripped all
  `input_backend=` args, removed 2 legacy-only tests); docstring-only cleanup in
  `test_pty_resize.py`/`test_pty_vt_relay.py`.

**Merge:** `git merge feature/supervisor-vt-input-relay` into `refactor/hybrid` (`f1efd03`). Two
conflict blocks in `pty_session.py` against hybrid's own tip (`0e037da`, which had separately added
temporary debug-logging instrumentation directly on `hybrid`) — both resolved by keeping the
feature branch's deletions, consistent with the plan's own deletion list naming
`_debug_log`/`_MOUSE_DEBUG_LOG` explicitly.

**Sign-off recorded in the plan doc itself**
(`docs/superpowers/plans/2026-07-22-supervisor-vt-input-relay.md`, commit `54174c2`): both
decisions, confirmation the paste helpers were dead code pre-deletion, and the Task 4.x audit
result (`pty_session.py` 848→539; repo-wide Phase 4 commit `+175/-1013`, net -838 lines).

**Cleanup:** `git branch -d feature/supervisor-vt-input-relay` initially refused ("not yet merged to
`refs/remotes/origin/feature/supervisor-vt-input-relay`, even though it is merged to HEAD") — git's
safety check compares against the branch's stale, pre-Phase-4 upstream, not current `HEAD`. Judged
safe (git itself confirmed full merge into `HEAD`, nothing pushed/touched remotely on that branch)
and used `git branch -D`, explaining the reasoning rather than silently forcing it. Worktree removed
via `finishing-a-development-branch`'s cleanup step.

This session's own doc-sync pass (this file plus `CLAUDE.md`/`docs/claudex/history.md`) is the final
step, per the mentor's explicit request to gather evidence and update the repo's local memory before
closing out.

## Why it changed

The mentor's Phase 5 live-validation report is the gate the plan itself set for Phase 4 — nothing in
the vt-relay backend could be deleted-around or the legacy backend retired until a human confirmed
it against a real Windows Terminal session. Once that passed, "thinner not smarter" — the plan's
own stated success criterion — required actually deleting the code the VT relay made obsolete, not
just leaving it inert behind the (now-removed) `local_input_backend` flag.

## Verification performed

- **Pre-merge, in the worktree:** full suite 114/114 after Phase 4 deletions.
- **Post-merge, on `refactor/hybrid`:** full suite independently re-verified 114/114.
- **This session, independently re-run from scratch (not trusted from the prior turn's
  self-report):** `git status` clean; `git log --graph` confirms the merge topology described above;
  `refactor/hybrid` up to date with `origin/refactor/hybrid` (already pushed — no push needed or
  performed this session). Each of the 9 test scripts run individually (this project's test runner
  is a hand-rolled `python tests/test_X.py` harness — pytest is not installed anywhere on this
  machine): `test_bot_mentions` 9, `test_core` 22, `test_pty_console_restore` 23, `test_pty_input` 2,
  `test_pty_resize` 4, `test_pty_vt_backend` 5, `test_pty_vt_relay` 5, `test_run_shutdown` 25,
  `test_snapshot` 19 — **114/114, 0 failures.**
  `python -m py_compile run.py hook_forward.py install_hooks.py claude_supervisor/*.py` clean.
- `git worktree list` confirms `.worktrees/supervisor-vt-input-relay` is gone; only the unrelated
  `autonomous-completion-trigger-framework` worktree remains.

## Remaining unknowns

None identified for this feature. All ten Phase 5 checklist items passed, both decisions the plan
refused to default are closed, the merge and cleanup are verified, and the tree is clean and pushed.

## Next safe action

None pending on the VT-input-relay migration. Other still-open threads, unrelated to this session's
work: the autonomous completion-trigger framework awaits the mentor's `finishing-a-development-
branch` choice (PR already pushed, link given); the Controls Academy roadmap awaits mentor review
before any scaffolding begins; the robot-code priority stack (SysId → drive Slot0 → chassis-PID) is
untouched.

## Handoff

- **Current state:** VT-input-relay migration is complete and merged. `refactor/hybrid` @ `54174c2`,
  clean working tree, up to date with `origin/refactor/hybrid`. `feature/supervisor-vt-input-relay`
  branch and its worktree are both deleted. Full Python suite green (114/114, 9 files).
- **Unverified items:** none for this feature.
- **Next safe step:** no action required on the supervisor/VT-relay work. If picking a next task,
  see the other open threads listed under "Next safe action" above, or `CLAUDE.md`'s `### Next up`
  section for the current priority list.
- **Things not to change:** do not resurrect the deleted `local_input_backend` config flag or the
  legacy `msvcrt`/mouse-translation code path — its removal was an explicit, signed-off mentor
  decision, not an oversight. Do not touch Controls Academy or the autonomous-completion-trigger-
  framework worktree/branch — both unrelated and untouched this session.
